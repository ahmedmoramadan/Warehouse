@model ExpireCommittee
@{
    ViewData["Title"] = "LastEXCMembers";
}



@{
    int rowIndex = 0;
}

@if (Model != null)
{
    <div class="container-fluid tender-parent text-center  mt-10">
        <h1 class="tender-header"> <i class="fa-solid fa-arrow-right"></i> Current Expire Committee Members</h1>
        <div class="tenderTable container">
            <div class="row text-center">
                <div class="col-md-12">
                    <form asp-action="ChooseHeadEXCMember">
                        <table class="table table-bordered" cellspacing="0" cellpadding="0">
                            <thead>
                                <tr class="table-primary">
                                    <th></th>
                                    <th>
                                        Mamber Name
                                    </th>
                                    <th>Phone Nember</th>
                                    <th>Select Head</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var i in Model.Members)
                                {
                                    rowIndex++;
                                    <tr class="@(rowIndex % 2 == 0 ?  "even-table" :  "odd-table")">
                                        <td>@rowIndex</td>
                                        <td>
                                            @Html.DisplayFor(modelItem => i.Member.Name)
                                        </td>
                                        <td>@i.Member.PhoneNumber</td>
                                        <td>
                                            <input type="radio" name="id" value="@i.Member.Id" required>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <div class="form-group d-flex justify-content-end">
                            <a value="Close" asp-action="Index" asp-controller="Home" class="btn btn-primary mx-2">Cancel</a>
                            <input type="submit" class="btn btn-primary" />
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <partial name="_CommitteePartialView"></partial>

}
@* <form asp-action="ChooseAcceptItem" asp-controller="AcceptItem">
                    <table class="table table-bordered" cellspacing="0" cellpadding="0">
                        <thead>
                            <tr class="table-pramiry tender-table">
                                <th>select</th>
                                <th>@Html.DisplayNameFor(model => model.Name)</th>
                                <th>@Html.DisplayNameFor(model => model.Description)</th>
                                <th>@Html.DisplayNameFor(model => model.Type)</th>
                                <th>@Html.DisplayNameFor(model => model.Price)</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr class="@(rowIndex % 2 == 0 ? "odd-table" : "even-table")">
                                    <!-- إضافة زر اختيار لكل عنصر -->
                                    <td>
                                        <input type="radio" name="id" value="@item.Id" required>
                                    </td>
                                    <!-- استخدام إدخال النص بدلاً من عرض النموذج -->
                                    <td>@item.Name</td>
                                    <td>@item.Description</td>
                                    <td>@item.Type</td>
                                    <td>@item.Price</td>
                                </tr>
                                rowIndex++;
                            }
                        </tbody>
                    </table>
                    
                </form> *@